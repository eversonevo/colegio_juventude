import 'package:colegio_juventude/app/modules/registration/widgtes/register_fields.dart';
import 'package:flutter/material.dart';

class RegisterTelephones extends StatelessWidget {

  const RegisterTelephones({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Row(
                  children: [
                 /*   Expanded(child: RegisterFields(label: 'TELEFONES', hintText: 'telefones', controllerTextField: controller.disciplinasController)),
                    Padding(
                      padding: const EdgeInsets.only(right: 8),
                      child: GestureDetector(onTap: (){}, child: const Icon(Icons.add)),
                    ),*/
                  ],
                );
  }
}